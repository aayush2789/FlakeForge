# Minimal deps for the *sandbox* image used by DockerTestRunner
# (LOCAL_IMAGE_NAME, default flakeforge-env:latest).
# Install only this in a slim Dockerfile; keep OpenEnv/FastAPI/ML off this image.
#
# The runner mounts your repo at /workspace and runs:
#   pytest <test_id> --tb=short -q --no-header
# Match what sample repos use (e.g. test_repos/*/requirements.txt).

pytest>=8.0.0
pytest-asyncio>=0.23.0
